"use client";

// La racine — désormais La Traversée (le rituel quotidien). L'ancien produit est
// débranché (archivé sur la branche archive/parcours-archetypes).

import { TraverseeApp } from "../traversee/components/TraverseeApp";

export default function Page() {
  return <TraverseeApp />;
}
