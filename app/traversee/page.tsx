"use client";

// La Traversée — deuxième produit, accessible depuis le menu d'IdentitX. Un
// monde à part (plein écran), branché sur son propre store isolé. On revient à
// l'ancien produit par « ← IdentitX ».

import { TraverseeApp } from "../../traversee/components/TraverseeApp";

export default function Page() {
  return <TraverseeApp />;
}
